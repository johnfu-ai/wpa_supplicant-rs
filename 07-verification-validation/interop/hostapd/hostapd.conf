# hostapd configuration for the interop harness.
#
# Wired-mode 802.1X Authenticator running on a bridge interface.
# The supplicant binary connects to this bridge via a second veth pair
# and receives EAPOL frames; hostapd proxies EAP to FreeRADIUS.

interface=br0
bridge=br0
driver=wired

# IEEE 802.1X options
ieee8021x=1
# hostapd's wired driver accepts EAPOL version 1 or 2 only (not 3);
# 802.1X-2020 EAPOL-v3 framing is a supplicant-side concern.
eapol_version=2
eap_server=0                     # Use external RADIUS server
eapol_key_index_workaround=0
eap_message=1

# RADIUS authentication server. hostapd parses this as a literal IP
# (no DNS resolution), so the FreeRADIUS container is pinned to a static
# IP in docker-compose.yml (10.5.0.10) and referenced here.
auth_server_addr=10.5.0.10
auth_server_port=1812
auth_server_shared_secret=testing123

# RADIUS accounting server
acct_server_addr=10.5.0.10
acct_server_port=1813
acct_server_shared_secret=testing123

# Periodic reauthentication
eap_reauth_period=3600

# MACsec / MKA (disabled for wired interop — hostapd does not support
# MKA over wired interfaces in the `wired` driver mode).
# macsec=0